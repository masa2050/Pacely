-- フェーズ7-3: 画面上の表示名として使うユーザーネームを追加する。
-- これまでDashboardではメールアドレスのローカル部を仮の表示名として使っていたが、
-- ユーザーが自由に編集できる名前を持たせるためusersテーブルにusernameカラムを追加する。
-- 未設定でも困らない項目(退会理由のように必須にする理由がない)なのでNULL許容にする。
ALTER TABLE users ADD COLUMN username varchar(50);
